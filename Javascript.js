/*
  Hamburger menu logic has been moved inline into each page's HTML.
  This file is intentionally empty to avoid duplicate event listeners
  that were causing the mobile overlay to block all link clicks.
*/
